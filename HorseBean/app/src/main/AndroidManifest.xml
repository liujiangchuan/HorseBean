<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ll.horsebean"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/FBaseAppTheme">

        <activity
            android:name=".demo.main.view.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".demo.database.activity.DatabaseActivity"/>
        <activity android:name=".demo.http.activity.HttpActivity"/>
        <activity android:name=".demo.ui.BannerActivity"/>
        <activity android:name=".demo.ui.GlideActivity"/>
        <activity android:name=".demo.recyclerview.view.RecyclerViewActivity"/>

        <meta-data
            android:name="CHANNEL"
            android:value="${CHANNEL}"/>
        <meta-data
            android:name="FLOG_DEBUG"
            android:value="${FLOG_DEBUG}"/>
        <meta-data
            android:name="FLOG_DEBUG_LEVEL"
            android:value="${FLOG_DEBUG_LEVEL}"/>
        <meta-data
            android:name="FLOG_OUT_TO_FILE"
            android:value="${FLOG_OUT_TO_FILE}"/>
        <meta-data
            android:name="FLOG_OUT_TO_FILE_LEVEL"
            android:value="${FLOG_OUT_TO_FILE_LEVEL}"/>
        <meta-data
            android:name="CRASH_CATCH"
            android:value="${CRASH_CATCH}"/>
        <meta-data
            android:name="STRICT_MODE"
            android:value="${STRICT_MODE}"/>
        <meta-data
            android:name="LEAK_CANARY"
            android:value="${LEAK_CANARY}"/>
    </application>

</manifest>
